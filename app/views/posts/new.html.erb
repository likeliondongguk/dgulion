<div id="wrapper">
  <%= render 'sticky/mainMenu' %>
  <%= render 'sticky/pageHeader', name: 'boards' %>
  <section>
    <div class="container">
      <div class="row">
        <div class="panel panel-default">
          <div class="panel-heading panel-heading-transparent">
            <div class="row">
              <div class="col-md-9">
                <h2 class="panel-title bold"><%= @category.name %> 새 글</h2>
              </div>
              <div class="col-md-3 text-right">
                <a href="<%= url_for(category_posts_path(@category)) %>" class="btn btn-reveal btn-sm btn-3d btn-blue">
                  <i class="fa fa-home"></i>
                  <span>뒤로가기</span>
                </a>
              </div>
            </div>
          </div>
          <%= render 'form', url: :new %>
        </div>
      </div>
    </div>
  </section>
  <%#= render 'sticky/footer' %>
</div>
<%= render 'home/script' %>